/// 闭包 closure
/// cargo run --bin 2_闭包_closure

/*
  
  什么是闭包（closure）

  闭包：可以捕获其所在环境的匿名函数。|| 闭包是个函数，这个函数可以赋值给一个变量 （JS中闭包是外部作用域可以访问内部作用域的变量，这个变量常驻内存）
  闭包：
    - 是匿名函数
    - 保存为变量、作为参数
    - 可以在一个地方创建闭包，然后在另一个上下文中调用闭包来完成运算
    - 可从其定义的作用域捕获值

*/

fn main() {
  
}
